# Agent SDK

A Spring Boot library that facilitates building LLM agents using LangChain4j.
It contains usefull classes to parse agent definition, call remote stdio agents (i.e. cagent)

## Capabilities

- ACP client to call remote stdio agents (i.e. cagent)
- Agent and skills definition parsing from Markdown files
- LLM to MCP server langgraph edges
